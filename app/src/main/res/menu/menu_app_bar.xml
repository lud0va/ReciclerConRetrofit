<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/add"
        android:icon="@drawable/add"
        android:title="add"
        android:contentDescription="descripcion"
        app:actionViewClass="androidx.appcompat.widget.AppCompatImageButton"
        app:showAsAction="never" />
    <item
        android:id="@+id/delete"
        android:icon="@drawable/cubo_de_basura"
        android:title="delete"
        android:contentDescription="descripcion"
        app:actionViewClass="androidx.appcompat.widget.AppCompatImageButton"
        app:showAsAction="never" />

    <item
        android:id="@+id/search"

        android:title="SEARCH"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        android:contentDescription="des2"
        app:showAsAction="never" />

    <item
        android:id="@+id/more"
        android:title="MORE"
        android:contentDescription="des1"
        app:showAsAction="never" />

</menu>